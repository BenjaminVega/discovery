spring:
  application:
    name: discovery-service
  cloud:
    config:
      uri: http://10.11.246.74:8088

---
spring:
  profiles: dev
  application:
    name: discovery-service
  cloud:
    config:
      uri: http://localhost:8088